# Docker-Flask-Mongodb-application

This is basic application on which we can do following operations

  - Registration
  - Storing a sentence
  - Getting stored sentence

It can also handles error as well for invalid password, out of tokens.
